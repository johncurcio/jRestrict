package parser;

import java.util.List;

public class Star implements Parser {
	public final Parser sp;
	
	public Star(Parser p) {
		sp = new OrdChoice(new Seq(p, this), Parser.eps);
	}
	
	@Override
	public List<Token> parse(Error err, List<Token> ent) {
		return sp.parse(err, ent);
	}
}