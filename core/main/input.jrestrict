files {
    Foo.java;
}
 
encloses{
    type: int, boolean, float;
    import: java.util.mooligan.get.Bat;
}

prohibits{
	import: java.util.*;
	operator: ==;
    modifier: private, static;
    vartype: double;
    type: int;
    returntype: void;
    loop: continue, do;
    branch: if;
}

# declaring all clauses that MUST be on my java file
requires{
    type: int, boolean, float;
    operator: +, ++, >>>=, >>>, <<, <=, --;
}