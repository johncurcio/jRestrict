# Input that shows some requirements not being fullfiled!

files {
    Foo.java;
    Bar.java;
}

prohibits{
	#operator: +, --, <<=, >>>=, *, /;
    #modifier: protected, static;
    #vartype: Object;
    #type: boolean;
    #loop: continue, do;
    #branch: if;
}
 
encloses{
    #returntype: int, boolean, float, void;
    #import: java.util.*;
}

# declaring all clauses that MUST be on my java file
requires{
    #type: int, void, float;
    loop: while;
}